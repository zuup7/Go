# 한국사 기말 — HANSA OS

`hansa.html`. 「한국사 첫 수업」 · 「오답 몬스터」 · 「연표 스프린트」 세 앱을 한 파일에
담았다. `build-hansa.py`가 `learn.html` + `monsters.html` + `index.html`을 읽어 만들어
낸다 — **직접 고치지 말고 원본을 고친 뒤 다시 만든다.**

```
python3 build-hansa.py
```

## 왜 합쳤나

배우는 앱과 확인하는 앱이 링크로 갈라져 있으면 오가지 않게 된다. 하나로 묶고 위쪽
작업 표시줄로 갈아 끼우면 「모르겠으면 첫 수업, 외웠으면 몬스터, 순서만 빠르게 돌릴 땐
스프린트」가 한 동작이 된다.

세 앱은 역할이 겹치지 않는다 — **첫 수업**은 처음 이해하는 곳, **오답 몬스터**는
문제로 확인하는 곳, **연표 스프린트**는 간격 반복으로 순서를 굳히는 곳이다.

## 어떻게 합쳤나

세 앱은 전역 변수(`S` `G` `render` `go` `stack` `esc` `UNITS` …), CSS 클래스
(`.card` `.note` `.fb` `.win` …), CSS 변수(`--tx` `--jade` `--bg2` …), 정적 id
(`app` `view` `title` `back` `clk` …)가 서로 겹친다. 그래서 이렇게 떼어 놓는다.

| 겹치는 것 | 푼 방법 |
|---|---|
| 전역 변수·함수 | 앱마다 통째로 IIFE 로 감싼다 |
| CSS 클래스·변수 | 앱마다 `.a-learn` / `.a-mon` / `.a-sprint` 아래로 민다. `:root`와 `body`도 그 뿌리로 옮긴다 |
| 정적 id | `app` → `Lapp` / `Mapp`, `stage` → `Sstage` 처럼 앞 글자를 붙인다. 마크업·JS·**CSS 선택자**·`querySelector("#id …")` 문자열까지 |
| 화면 안에서 만드는 id (`nx` `opts` …) | **쉬는 앱은 DOM 에서 뗀다** — `getElementById`가 못 찾는다 |
| `popstate` · 단축키 | `window.__hansa` 로 켜져 있는 앱만 반응하게 막는다 |
| 돌아가는 시계 | 갈아 끼울 때 `__hansaPause` 로 멈추고 돌아오면 `__hansaResume` 로 되살린다 (아래) |

요소 참조는 떼어 놔도 살아 있으므로, 다시 붙이면 보던 화면 그대로 이어진다.

`body`의 글자색·서체를 앱 뿌리로 옮기는 게 중요하다. 안 옮기면 셸 색을 물려받아
어두운 앱의 제목이 배경에 묻힌다.

## 작업 표시줄

로고 · 앱 단추 셋 · 시계. 앱 단추에는 배지가 붙는다 —
「첫 수업」은 오답 + 오늘 복습할 수업 수, 「몬스터」는 남은 몬스터 수,
「연표」는 오답 노트에 담긴 항목 수. 각 앱의 `localStorage`를 4초마다 읽어 그린다.
좁은 화면에서는 단추 줄이 가로로 밀린다.

- 켜져 있는 앱을 다시 누르면 그 앱의 홈으로 (독 아이콘과 같은 감각).
- 마지막에 쓰던 앱은 `hansa-app`에 남아 다음에 열 때 그대로 뜬다.
- 앱 안에 심어 둔 `[data-hansa-go]` 단추로도 건너간다 — 「첫 수업」 홈 아래
  「오답 몬스터 열기 / 연표 스프린트 열기」, 「오답 몬스터」 홈 아래 두 앱,
  「연표 스프린트」의 `나` 탭 위쪽에 두 앱.

## 저장

앱마다 그대로다 — `hansa-learn-v1` · `hansa-monster-v1` · `hansa-sprint-v2`.
따로 쓰던 세 앱의 진도가 합친 뒤에도 이어진다. 셸은 `hansa-app` 하나만 더 쓴다.

## 갈아 끼울 때 시계를 멈춘다

「오답 몬스터」에는 초를 세는 판이 셋 있다 — 스피드 O·X(60초), 짝 맞추기(경과 시간),
먼저 온 쪽(60초). 쉬는 앱을 DOM 에서 떼어도 `setInterval` 은 계속 돌기 때문에, 다른
앱을 보는 동안 60초가 다 흘러 판이 혼자 끝나 버렸다.

그래서 시계를 「시작」과 「돌리기」로 나누고(`startClock` / `runClock`,
`runPairTimer`, `runDuelTimer`), 셸이 앱을 갈아 끼울 때 `window.__hansaPause[k]()` 로
멈췄다가 돌아올 때 `window.__hansaResume[k]()` 로 되살린다. 짝 맞추기는 흐른 시간을
재므로 떠난 때를 적어 두었다가 돌아올 때 `t0` 를 그만큼 민다.

곁들여 화면을 떠나면 시계가 스스로 멈추게 했다(`stack` 맨 위가 그 화면이 아니면
정지). 합치기 전에도 O·X 판을 나가면 시계가 계속 돌다 남의 화면 위에 결과를
그렸다.

## 정확한 연도는 묻지 않는다

시험은 몇 년인지가 아니라 **무엇이 먼저였나**를 본다. 그래서

- 「○○은(는) 몇 년의 일일까?」 문제 **69개를 출제에서 뺐다**(`usable()`).
  기록이 깨지지 않게 데이터에서 지우지는 않고 풀에서만 막는다. 그때 잡힌 몬스터는
  불러올 때 놓아 준다.
- 「짝 맞추기」를 사건↔연도에서 **인물↔한 일**로 바꿨다. 도감 인물 32명을 그대로 쓴다.
- 「첫 수업」의 연표 탭은 **「사건 흐름」**이 되었다. 단원 경계를 지우고 88개 사건을
  일어난 순서대로 한 줄에 세우고, 시기(1910년대 · 1920년대 · 1930~40년대)와
  갈래(일제 · 우리 · 세계)로만 나눈다. 연도는 기본으로 접혀 있고(`·····`),
  궁금할 때만 「연도 펴기」나 그 칸을 눌러 편다.

연표 세우기 · 순서 배열 · 인과 사슬처럼 **흐름을 묻는 것들은 그대로 둔다.** 오히려
그쪽이 시험 범위다.

## 합친 뒤에 들어온 것

세 앱은 각자의 파일에서 자라고, `build-hansa.py` 를 다시 돌리면 그대로 따라온다.
이번에 들어온 것 —

- **연표 스프린트 합류** — 세 번째 앱. 위 참고.
- **서술형 연습** — 교과서 본문이 그대로 답이 되는 18문항. 쓴 글에서 핵심어를 찾아
  채점하고, 모범 답안에서 빠뜨린 말을 빨갛게 짚어 준다(`LEARN.md` 「서술형 연습」).
- **갈아 끼울 때 시계 멈추기** — 위 참고.
- **수업 중 ← 이전** — 카드·퀴즈를 한 칸씩 되돌린다. 퀴즈에서 되돌아가면 점수도
  같이 되돌린다(`LEARN.md` 「화면」).
- **연표 한 줄로** — 위 참고.
- **암기 장치 가리고 떠올리기** — 본문을 가려 두고 스스로 매기게 한다. 헷갈린 것이
  위로 올라오고 「덜 외운 것만」으로 좁힐 수 있다(`LEARN.md` 「암기 장치」).

## 성능

세 앱의 `README.md` · `LEARN.md` · `MONSTERS.md` 에 적은 두 가지가 여기에도 그대로
적용된다 — 글꼴을 렌더에서 빼고(첫 화면 12,931ms → 202ms), 긴 목록은
`content-visibility:auto` 로 화면 밖을 건너뛴다.

합친 앱은 쉬는 앱들을 DOM 에서 떼어 두므로, 안 보이는 앱의 화면은 애초에
배치 비용이 들지 않는다. 앱이 셋이 되어도 한 번에 붙어 있는 건 하나뿐이다.
