# 한국사 기말 — HANSA OS

`hansa.html`. 「한국사 첫 수업」과 「오답 몬스터」를 한 파일에 담았다.
`build-hansa.py`가 `learn.html` + `monsters.html`을 읽어 만들어 낸다 — **직접 고치지 말고
원본을 고친 뒤 다시 만든다.**

```
python3 build-hansa.py
```

## 왜 합쳤나

배우는 앱과 확인하는 앱이 링크 두 개로 갈라져 있으면 오가지 않게 된다. 하나로 묶고
위쪽 작업 표시줄로 갈아 끼우면 「모르겠으면 첫 수업, 외웠으면 몬스터」가 한 동작이 된다.

## 어떻게 합쳤나

두 앱은 전역 변수(`S` `G` `render` `go` `stack` …), CSS 클래스 13개(`.card` `.note`
`.fb` …), CSS 변수 19개(`--tx` `--jade` `--bg2` …), 정적 id 5개(`app` `view` `title` …)가
겹친다. 그래서 이렇게 떼어 놓는다.

| 겹치는 것 | 푼 방법 |
|---|---|
| 전역 변수·함수 | 앱마다 통째로 IIFE 로 감싼다 |
| CSS 클래스·변수 | 앱마다 `.a-learn` / `.a-mon` 아래로 민다. `:root`와 `body`도 그 뿌리로 옮긴다 |
| 정적 id | `app` → `Lapp` / `Mapp` 처럼 앞 글자를 붙인다. 마크업·JS·**CSS 선택자** 셋 다 |
| 화면 안에서 만드는 id (`nx` `opts` …) | **쉬는 앱은 DOM 에서 뗀다** — `getElementById`가 못 찾는다 |
| `popstate` · 단축키 | `window.__hansa` 로 켜져 있는 앱만 반응하게 막는다 |

요소 참조는 떼어 놔도 살아 있으므로, 다시 붙이면 보던 화면 그대로 이어진다.

`body`의 글자색·서체를 앱 뿌리로 옮기는 게 중요하다. 안 옮기면 셸 색을 물려받아
어두운 앱의 제목이 배경에 묻힌다.

## 작업 표시줄

로고 · 앱 단추 둘 · 시계. 앱 단추에는 배지가 붙는다 —
「첫 수업」은 오답 + 오늘 복습할 수업 수, 「오답 몬스터」는 남은 몬스터 수.
각 앱의 `localStorage`를 4초마다 읽어 그린다.

- 켜져 있는 앱을 다시 누르면 그 앱의 홈으로 (독 아이콘과 같은 감각).
- 마지막에 쓰던 앱은 `hansa-app`에 남아 다음에 열 때 그대로 뜬다.
- 앱 안에 심어 둔 `[data-hansa-go]` 단추로도 건너간다 — 「첫 수업」 홈 아래
  「오답 몬스터 열기」, 「오답 몬스터」 홈 아래 「한국사 첫 수업」.

## 저장

앱마다 그대로다 — `hansa-learn-v1` · `hansa-monster-v1`. 따로 쓰던 두 앱의 진도가
합친 뒤에도 이어진다. 셸은 `hansa-app` 하나만 더 쓴다.

## 정확한 연도는 묻지 않는다

시험은 몇 년인지가 아니라 **무엇이 먼저였나**를 본다. 그래서

- 「○○은(는) 몇 년의 일일까?」 문제 **69개를 출제에서 뺐다**(`usable()`).
  기록이 깨지지 않게 데이터에서 지우지는 않고 풀에서만 막는다. 그때 잡힌 몬스터는
  불러올 때 놓아 준다.
- 「짝 맞추기」를 사건↔연도에서 **인물↔한 일**로 바꿨다. 도감 인물 32명을 그대로 쓴다.
- 「첫 수업」의 연표 탭은 **「사건 흐름」**이 되었다. 연도는 기본으로 접혀 있고
  (`·····`), 궁금할 때만 「연도 펴기」나 그 칸을 눌러 편다.

연표 세우기 · 순서 배열 · 인과 사슬처럼 **흐름을 묻는 것들은 그대로 둔다.** 오히려
그쪽이 시험 범위다.
