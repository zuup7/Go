# 한국사 첫 수업

내용을 처음 배우는 사람을 위한 앱. `learn.html` 단일 파일이며 vanilla JS로 돌아간다.

앞서 만든 두 앱(「연표 스프린트」, 「오답 몬스터」)은 **이미 아는 것을 확인하는** 도구다.
이 앱은 그 앞 단계, 곧 **처음 이해하고 외우는** 단계를 맡는다.

## 가르치는 방식

사건을 나열하지 않고 **원인 → 사건 → 결과**로 잇는다. 모든 `fact` 카드에는 `why`(왜
그랬나)와 `so`(그래서 어떻게 됐나)가 붙어 있고, 레슨마다 그 흐름을 한 줄로 요약하는
`chain` 카드가 들어간다. 이 구조가 그대로 서술형 답안이 된다.

한 화면에 한 가지만 보여 주고, 배운 직후 `check` 카드로 스스로 떠올리게 한 뒤 답을
공개한다(답은 흐리게 가려 두었다가 버튼을 누르면 드러난다). 레슨 끝에는 4문항짜리
마무리 퀴즈로 인출 연습을 시킨다.

## 데이터

`LESSONS` 배열 하나에 33개 수업이 들어 있다. 교과서 Ⅰ단원(8~63쪽) 소단원 18개를
순서대로 덮는다 — 8~33쪽(`s1`~`s8`)이 L1~L20, 34~63쪽(`s9`~`s18`)이 L21~L33.

| 필드 | 내용 |
|---|---|
| `id` `u` `part` `title` `sc` | 수업 식별 · 단원 · 부 제목 · 제목 · 장면 그림 키 |
| `hook` | 첫 화면에 나오는 도입 문장 |
| `cards[]` | 수업 본문 |
| `quiz[]` | 마무리 퀴즈 4문항 |

수업 밖에 두 벌이 더 있다 — 내신 실전 `EXAM` 140문항, 서술형 `ESSAY` 28문항.

연표와 통치↔저항 대조표의 줄 165개는 모두 수업의 `fact` 카드에서 나온다. 그래서 어느
줄을 눌러도 그 수업의 그 카드로 간다. 34~63쪽 사실 72개는 먼저 `TL_EXTRA`로 들어왔는데,
지금은 같은 단원·같은 이름의 `fact` 카드가 있으면 건너뛰고, 수업에 없는 사실만 채우는
예비로 남는다(그런 줄은 점선·기울임, 「수업 준비 중」).

`fact` 카드는 보통 단원의 줄(`TRACK[u]`)을 따르지만, 단원과 결이 다른 사실은 `tk`로
줄을 바꾼다 — 예: 농촌 진흥 운동·조선사 편수회·사찰령은 `rule`, 연해주 한인 강제 이주와
카이로·얄타·포츠담은 `world`. 새 수업 장면 그림은 `bomb` `school` `city` `scale` `book`
`pen` `dawn`.

첫 화면의 새싹은 끝낸 수업 비율로 자라고(20단계), 「N개 수업」 같은 문구는 `LESSONS.length`
에서 세므로 수업을 더해도 고칠 곳이 없다.

카드 유형 7가지 — 총 368장.

| `t` | 하는 일 | 개수 |
|---|---|---|
| `story` | 배경 이야기로 맥락 깔기 | 24 |
| `fact` | 핵심 사실 + `why` + `so` | 165 |
| `chain` | 인과 사슬 (애니메이션) | 34 |
| `hint` | 두문자 등 암기 장치 | 37 |
| `check` | 스스로 떠올린 뒤 답 공개 | 57 |
| `pick` | 2지선다 즉시 확인 | 36 |
| `compare` | 헷갈리는 둘 비교표 | 15 |

## 화면

`홈` → `수업`(카드 → 퀴즈 → 마무리) · `오답 노트` · `내신 실전` · `서술형 연습` ·
`다시 훑기` · `복습 세션` · `찾기`.

수업 안에서는 **← 이전** 단추로 한 칸씩 되돌아간다(PC는 `←` 키). 카드에서는 앞
카드로, 답을 이미 깐 `check` 카드에서는 **답부터 다시 가리고**, 마무리 퀴즈 첫
문제에서는 마지막 카드로 돌아간다. 퀴즈 안에서 되돌아가면 그때 매긴 점수와
기록(`qok` · `qlog`)도 같이 되돌린다 — 뒤로 갔다 오는 것으로 점수가 부풀지 않는다.
첫 카드에서는 되돌아갈 자리가 없으니 단추를 아예 내지 않는다(`canBack()`).

**다시 훑기**는 탭 두 개로 나뉜다.

- **암기 장치** — 마친 수업의 `hint`와 `chain`을 **가리고 떠올리는** 화면. 아래 참고.
- **사건 흐름** — 연표. 아래 참고.
- **통치↔저항** — 시기별 대조표. 아래 참고.

## 연표 — 한 줄로 쭉

`fact` 카드 88장에는 전부 `y`(연도)가 붙어 있다. 단원 경계를 지우고 **일어난 순서대로
한 줄에** 세운다. 1910~1944년.

맨 위에 시기 세 칸(1910년대 무단 통치 → 1920년대 문화 통치 → 1930~40년대 민족 말살)을
띠로 깔아 전체 모양을 먼저 보여 주고, 내려가면서 그 시기의 검은 머리표가 다시 나온다.
시기는 통치 방식이 바뀐 두 지점(3·1 운동, 만주 사변)에서 끊었다.

줄마다 **갈래**를 색과 꼬리표로 나눈다. 일제가 한 일(빨강 · `일제`), 우리가 한 일
(청록 · `우리`), 바깥 세계(파랑 · `세계`). 단원 `s2~s4`가 통치, `s5~s8`이 저항,
`s1`이 세계다(`TRACK`). 둘이 어떻게 맞물리는지가 이 화면의 요점이다.

- **설명 접기** — 켜면 사건 이름만 남는다. 이름만 보고 무슨 일이었는지 떠올려 보는
  용도. `S.tlDesc`.
- **연도 가리기** — 연도를 `·····`로 덮는다. 스스로 맞혀 본 뒤 그 연도 칸만 눌러
  하나씩 까 볼 수 있다(전체를 다시 켜지 않아도 된다). 기본값이 「가림」이다 —
  시험은 연도가 아니라 순서를 묻기 때문이다. `S.tlHide`.
- **갈래 거르기** — 전체 · 일제의 통치 · 우리의 저항 · 바깥 세계. 세로로 흘러내려
  정작 연표를 밀어내지 않도록 가로로 미는 한 줄 띠로 만들었다.
- **바로 가기** — 아무 줄이나 누르면 그 사건을 다룬 수업의 그 카드로 곧장 간다
  (`jumpCard()` — 찾기 결과와 같은 경로를 쓴다).
- 네모 표시가 찬 줄은 이미 배운 수업, 빈 줄은 아직 안 배운 수업이다.

같은 해 안에서는 교과서 차례를 그대로 둔다. `Array.prototype.sort`가 안정 정렬이고
`LESSONS`가 이미 단원 순서라 연도만 비교하면 된다.

## 통치 ↔ 저항 대조표

Ⅰ단원은 「일제가 한 일(01)」과 「우리가 한 일(02·03)」이 나란히 놓인 단원이고, 시험은 그
둘을 같은 시기에 겹쳐 놓고 묻는다 — 「1920년대에 일제는 ○○을 했고 우리는 △△로
맞섰다」. 그래서 연표와 **같은 자료**로 시기 × 갈래 표를 세웠다.

- 새로 적은 내용이 하나도 없다. `timeline()` 이 뽑아 둔 사실 카드를 `eraOf()` 와
  `TRACK` 으로 갈라 놓을 뿐이라, 카드가 늘거나 연도를 고치면 표도 따라 바뀐다.
- 시기마다 두 칸(일제 · 우리)을 나란히 두고, 바깥 세계는 아래 띠에 붙인다.
- 칸을 누르면 그 수업의 그 카드로 간다. 아직 안 배운 것은 점선으로 남는다.
- 칸 길이는 일부러 맞추지 않았다. 1910년대 오른쪽이 긴 것(31개)은 3·1 운동과 임시
  정부가 모두 거기 걸리기 때문이고, 그 불균형 자체가 단원의 모양이다.

## 암기 장치 — 가리고 떠올리기

읽기만 하면 아는 것 같지만 시험지 앞에서는 안 나온다. 그래서 이 화면은 본문을 먼저
가린다.

- 암기 장치의 **키**(두문자 등)나 인과 사슬의 **제목**만 보여 주고, 본문 자리에는
  빗금 친 칸을 놓는다. 인과 사슬은 첫 고리 하나만 미끼로 보여 준다 — `대공황(1929) → ?`
- 속으로 답한 다음 칸을 누르면 본문이 열리고 **헷갈려 / 기억났어** 두 단추가 뜬다.
- 매긴 결과는 `S.mem[레슨id#종류번호] = 0 | 1`. **헷갈린 것이 맨 위로** 올라오고,
  다 외운 것은 맨 아래로 내려간다.
- 머리에 헷갈림 · 기억함 · 아직 개수를 띄운다. **덜 외운 것만** 으로 좁히면 시험
  직전에 그것만 돌릴 수 있다.
- **다 펴 놓기** 로 예전처럼 전부 펼쳐 놓고 훑을 수도 있다. 가리기·좁히기 설정은
  `S.mmHide` · `S.mmOnly` 에 남아 다음에 들어와도 그대로다.
- 헷갈린다고 매긴 게 있으면 **홈에 배너**로 뜬다. 누르면 「덜 외운 것만」을 켠 채로
  열린다 — 매기고 끝나면 소용이 없다.

홈의 30초 복습 카드도 같은 이유로 본문을 흐려 두고, 누르면 드러난다.

## 복습 세션 (간격 반복)

수업을 마치면 다시 물어볼 날짜가 잡힌다. 마무리 퀴즈를 75% 이상 맞히면 간격이
늘고, 못 맞히면 처음으로 되돌아간다.

`DUE_DAYS = [1, 3, 7, 16]` — 1일 → 3일 → 7일 → 16일.

`S.due[레슨id] = {n, at}`. `n`은 간격 단계, `at`은 다시 볼 날(1970년 이후 일 수).
오늘이 `at`을 지난 수업이 홈 배너와 독 배지에 뜨고, **복습 세션**은 그 수업들에서
2문항씩 뽑아 한 번에 낸다.

옛 저장본에 `due`가 없으면 불러오는 시점에 마친 수업 전부를 내일로 잡아 준다.

## 시험일과 진도

시험일을 넣으면 D-day와 함께 **하루에 몇 개씩 해야 끝나는지** 계산해 준다
(`pace()` — 남은 수업 ÷ 남은 날). 날짜는 `S.dday`에 `YYYY-MM-DD`로 담긴다.

## 찾기

창 제목 표시줄의 &#9906; 버튼, 또는 PC에서 `/` 키.

수업 카드 368장 + 수업 퀴즈 132문항 + 내신 140문항 + 서술형 28문항 + 인물·단체 113개를 한 색인
(수업 도입 33개 포함 **814항목**)으로 묶어 낱말로 찾는다. 찾은 낱말은 노랗게 칠해서 보여 준다. 수업 결과를 누르면 그 카드로
가고, 퀴즈·내신 결과를 누르면 답과 해설이 모달로 뜬다.

수업 시간에 처음 듣는 말이 나왔을 때 그 자리에서 치라고 만든 화면이다.

## 키보드 (PC)

| 키 | 하는 일 |
|---|---|
| `1`~`5` | 선택지 고르기 |
| `Enter` `Space` | 다음 / 확인 |
| `←` `→` | 이전 / 다음 (수업 카드·문항) |
| `/` | 찾기 |
| `Esc` | 모달 닫기 · 홈으로 |

입력칸에 글을 치는 동안에는 단축키가 먹지 않는다. 단답형에 "3부"를 쳐도 3번
선택지가 눌리지 않는다. 다만 `Esc`만은 입력칸 안에서도 통한다 — 찾기 창에서
손을 뗄 길이 있어야 하기 때문이다.

## 오답 노트

수업 퀴즈, 2지선다, 내신 실전 어디서 틀리든 `S.wrong[레슨#유형인덱스]`에 쌓인다. 저장하는 건 위치뿐이고
문제 본문은 `wItem()`이 `LESSONS`에서 그때그때 되살린다. 데이터를 고쳐도 오답 기록이
깨지지 않는다.

`kind`는 `q`(수업 퀴즈) · `p`(2지선다) · `e`(내신 실전)이고, `e`는 `idx`에 문항
id가 들어간다.

`{lid, kind, idx, miss, at, chose, fix, okAt}` — `chose`는 그때 고른 답이라 노트에서 "내 답"으로
보여 준다. `miss`가 2 이상이면 빨간 점으로 표시한다.

**맞혀도 지우지 않는다.** 어디서든 다시 맞히면 `wOk()`가 `fix`를 1 올리고 「맞힘 ✓」만 붙인다.
또 틀리면 `wAdd()`가 `miss`를 올리고 `fix`를 0으로 되돌린다. 지우는 건 사용자가 한다(`wClear`/직접 `delete`).
- 오답 노트: 칩 `전체 / 아직 못 맞힘 / 맞힘 ✓`(`WTAB`). 「다시 풀기」는 지금 칩 범위로 낸다(`WQF`).
- 카드마다 「지우기」 — 곧바로 지우고 5초 동안 「되돌리기」 줄(`undoBar`)을 띄운다.
- 「맞힌 것 N개 지우기」 · 「전부 지우기」는 두 번 눌러야 한다(`armBtn`, 3초 안). `confirm()`은 샌드박스에서
  막힐 수 있어서 쓰지 않는다.
- 다시 풀기: 못 맞힌 것을 앞에 둔다. 맞히면 「다 외웠어 · 오답에서 지우기」 버튼, 결과 화면엔
  「이번에 맞힌 N개 오답에서 지우기」(`W.okKeys`)가 있다.
- 약점 리포트는 `fix>0`인 오답을 세지 않는다. 한 장 요약은 못 맞힌 것 먼저, 맞힌 것엔 ✓를 붙여 싣는다.

## 선지 섞기

원본 데이터는 정답이 대개 첫 칸에 적혀 있었다 — 수업 퀴즈 80문항과 2지선다 카드 20장은
**전부** 첫 칸, 내신 5지선다 38문항과 순서 배열 8문항도 전부 ①. 앱이 선지를 섞지 않았으므로
「무조건 첫 번째」만 눌러도 수업 퀴즈는 100점이었다.

이제 문제를 낼 때마다 섞은 **사본**을 만든다(`mixQ`). 원본은 건드리지 않는다.

- 5지선다·옳지 않은 것·순서 배열·수업 퀴즈·2지선다: 선지 순서를 섞는다.
- 〈보기〉 조합형: 선지(「ㄱ, ㄴ」 …)는 그대로 두고 **ㄱ~ㄹ 진술의 자리**를 섞은 뒤, 정답
  조합이 선지에 있는 배치만 쓴다. 해설 속 ㄱ~ㄹ 도 새 자리에 맞게 바꿔 적는다.
- 고른 번호는 원본 번호로 되돌려 오답 노트에 저장한다(`unmix`). 오답 노트는 원본을 보기 때문이다.
- 한 문제를 보는 동안에는 같은 배치를 유지한다(다시 그려도 섞이지 않게 수업 상태에 붙여 둔다).

적용한 곳은 다섯 군데 — 수업 마무리 퀴즈, 2지선다 카드, 내신 실전, 오답 다시 풀기, 복습 세션.
수천 번 섞어 보고 정답 문자열·되돌린 번호·〈보기〉 진술이 한 번도 어긋나지 않는 것을 확인했고,
①만 찍었을 때의 정답률은 모든 유형에서 20% 안팎(2지선다는 50%)으로 떨어졌다.

## 약점 리포트

홈 '내신 대비'의 「약점 리포트」(`vWeak`). 흩어진 결과를 단원 18개별로 모아 약한 단원부터 세운다.

- **단원별 정답률.** 답할 때마다 `uHit(단원, 맞음)`이 `S.ust[u]={n,ok}`에 쌓는다. 기록하는 곳은 수업 마무리 퀴즈 ·
  2지선다 카드 · 내신 실전 · 오답 다시 풀기 · 인물·단체 카드 · 흐름 맞추기(흐름 세트는 한 번에 맞혔는지, 사건 카드는 카드마다)다.
  `S.ust`가 없는 단원은 수업 퀴즈 마지막 점수(`S.quiz`)로 대신한다. 다섯 번 미만이면 정답률은 비워 둔다.
- **헷갈림.** 암기 장치 「헷갈림」(`S.mem`), 프린트 빈칸 「헷갈림」(`S.pmark`), 킵(`S.keep`), 흐름 맞추기에서 틀린 사건
  (`S.ord.fm`) · 다시 풀 흐름, 인물·단체 `nbox===0`을 단원별로 센다.
- **점수 · 배지.** (1−정답률)·3 + 오답·0.5 + 헷갈림·0.4 + 안 배운 수업 비율·1.5로 줄을 세운다.
  배지는 글자로 붙인다: 「약함」(정답률 60% 미만 또는 오답+헷갈림 5 이상) · 「보통」 · 「좋음」 · 「기록 부족」.
- **바로 가기.** 단원을 펼치면 「수업 보기」(안 끝낸 첫 수업), 「이 단원 오답 풀기」(`WU`로 `vWQuiz`를 단원 필터),
  「인물·단체 카드」(`nDeal(null, 단원)`), 「흐름 맞추기」(그 단원 첫 흐름 세트)가 나온다.

## 시험 전날 한 장 요약

「시험 전날 한 장 요약」(`vSheet`). 약한 단원 셋 · 선생님 밑줄 · 교과서와 다른 점 · 내가 킵한 것 · 남은 오답 ·
자주 틀린 사건 · 헷갈린 인물·단체 · 다시 볼 흐름 · Ⅰ단원 흐름 24개를 한 장에 모은다(`sheetParts`). 비어 있는 칸은 숨긴다.
「인쇄하기」는 숨은 iframe에 흑백 인쇄용 문서를 써 넣고 `print()`를 부른다. 합본의 `.a-learn` 스타일 범위와 상관없이
인쇄된다. 시험일이 오늘이나 내일이면 홈 위쪽에 「한 장 요약으로 마지막 점검하기」 띠가 뜬다.

## 흐름 맞추기

홈 '복습'의 「흐름 맞추기」(`vOrd` 메뉴 → `vORun` 판 · `vOStack` 쌓아 가기). 카드를 **끌어서** 순서나 칸을 맞춘다.
연도는 가려 두고 흐름만 맞추며, 끝나면 연도와 '다음으로 이어지는 까닭'을 화살표로 이어 보여 준다.

| 모드 | 재료 | 방식 |
|---|---|---|
| 흐름 세트 | `FLOWRAW` 24세트(4~6장, `[사건, 때, 까닭]`) | 순서 판. 틀린 세트(`S.ord.sets[id].b=0`) → 안 해 본 것 → 오래된 것 순으로 다음 세트를 고른다 |
| 시대 섞기 | 연표 사건 139개(`oRows`) | 서로 다른 단원 · 서로 다른 해 5장 순서 판 |
| 통치 ↔ 저항 | 같은 연표, 갈래가 `rule` / `resist` | 세 시기 칸(1910~1919 · 1920년대 · 1930~1945)에 일제 정책 두 개씩 걸어 두고, 우리 쪽 사건 6장을 알맞은 칸에 넣는다 |
| 쌓아 가기 | `BLOCKS` 여섯 묶음 + 연표 | ① 큰 묶음 여섯 개를 경계 사건(▶ 여는 · ◀ 닫는)만 보고 순서대로 세움 → ② 한 판 5장씩 사건을 알맞은 묶음에 넣음(한 번에 맞힌 것만 쌓임, `S.ord.stk.p`) → ③ 네 개 넘게 쌓인 묶음은 그 안의 순서도 맞춤 |

**시기 나누기(`eraOfRow`).** 연표 · 대조표 · 통치↔저항은 3·1 운동과 만주 사변에서 시기를 끊는다. 1919년은 햇수만으로
갈리지 않아서 단원으로 나눈다 — 3·1 운동 자체(s6)와 파리 강화 회의(s1)는 무단 통치 칸, 3·1 운동 뒤에 생긴 것
(문화 정치 시작 s3 · 신흥 무관 학교 s5 · 임시 정부 s7 · 의열단 · 강우규 s9)은 다음 칸(`ERA1919`). 예전엔 1919년을 통째로
무단 통치에 넣어서 교통국 · 연통제 · 의열단이 무단 통치 칸 정답으로 나왔다. 연표 스프린트도 같은 규칙(`eraOfF`)을 쓴다.
교과서대로 민족 말살 통치는 「만주 사변 이후」다(흐름 세트 f24도 그렇게 고침).

**대략의 시기(`YAPX`).** 교과서가 한 해로 못 박지 않은 사실(우민화 교육 · 기간 시설 건설 · 3부의 성립 · 3부 통합 운동 ·
자치 운동 · 황푸 군관 학교 입교 · 혁명적 농민·노동 조합 · 농촌 진흥 운동 · 남면북양 정책 · 조선학 운동 · 신채호의 고대사 연구 ·
백남운 · 내선일체 · 신사 참배 강요 · 인력 수탈 · 공출과 배급제 · 3부의 성격)은 카드와 연표에 숫자 대신 「1920년대 중반」 같은
시기를 보여 준다. 숫자 y는 연표 자리만 잡는다. 연도를 겨루는 판(시대 섞기 · 묶음 안 순서)에서는 빼고, 쌓아 가기 · 통치↔저항은
시기가 한 칸으로 정해질 때만 낸다. 연표 스프린트(`APX` — 「몇 년?」 문제 · 연표 정렬에서 뺌)와 오답 몬스터 해설도 같은 말을 쓴다.

**물산 장려 운동 = 1923.** 교과서는 연도 없이 「총독부가 관세를 없애려 하자 → 물산 장려 운동」으로 쓴다. 그래서 1920(평양 시작)이
아니라 1923(전국 확산)에 두어 연표에서 관세 철폐 뒤에 오게 했다(같은 해 안에서는 단원 차례라 s3 관세가 먼저).

**같은 해 안의 순서(`YM`).** 연표는 같은 해 안을 단원 차례로 두면 시간이 거꾸로 읽히는 곳이 많았다(1919 고종 서거가 2·8 뒤,
1941 태평양 전쟁이 건국 강령 앞 등). 사건이 여럿인 해(`YM_YEARS`)는 월일(MMDD)로 줄 세운다 — 정렬에만 쓰고 화면엔 안 보인다.
대략 시기 항목은 그 해 끝, 1923 관세 철폐는 교과서 인과대로 맨 앞. 연표 스프린트도 같은 표를 쓴다(`ymOfF`).

**큰 묶음(`BLOCKS`).** 처음엔 다섯 칸이었는데 「1920년대」 한 칸에 사건 53개가 몰렸고, 「대공황과 만주 침략」이라는
칸 이름과 달리 대공황(1929)은 그 앞 칸에 들어가 헷갈렸다. 지금은 여섯 칸이다 —
1910~1918 무단 통치 · 1919 한 해 · 1920~1925 3·1 운동 뒤 여러 갈래로 · 1926~1929 좌우가 손잡다 ·
1931~1936 만주 사변과 한중 연합 · 1937~1945 민족 말살과 광복 준비. 칸마다 `a`(▶ 문을 여는 사건)와 `z`(◀ 닫는 사건),
`g`(그때 일제 통치), `k`(대표 사건)를 적어, 소개 화면 · 1단계 카드 · 넣기 칸 · 쌓인 판에 경계 사건을 늘 보여 준다.
1919 칸의 경계는 교과서에 달이 없어서 「1919 초 / 1919 말」로 적었다. 묶음을 바꾸면 `BLOCKV`를 올린다 —
예전 1단계 기록(`stk.o`)만 지우고, 채운 사건(`stk.p`)은 그대로 둔다.
넣기 판은 칸이 길어서 「남은 카드」 칸을 화면 아래에 붙여 둔다(`.otray.stick`). 카드 밑에는 단원 이름을 작게 보여 준다.

- **조작.** 끌어서 다른 카드 위에 놓으면 **자리 바꿈**, 칸 위에 놓으면 **넣기**. 두 장을 차례로 누르거나
  카드 → 칸을 눌러도 된다(폰에서 드래그가 불편할 때). 끄는 동안 화면 가장자리에 가면 저절로 스크롤된다.
  pointer 이벤트 하나로 마우스와 손가락을 함께 받는다(`oBind`). 끄는 카드의 사본(`.oghost`)은 `view` 안에 둔다.
  hansa 합본에서 스타일 범위가 `.a-learn`으로 묶여 있어서다.
- **두 번의 기회.** 첫 확인에서 제자리인 카드는 초록으로 잠기고, 나머지만 다시 옮긴다. 두 번째도 틀리면
  정답으로 바꿔 놓고, 틀렸던 카드를 빨갛게 보여 준다.
- **기억.** 한 번이라도 제자리에 못 놓은 사건은 `S.ord.fm`에 쌓여 다음 판에 먼저 나온다. 흐름 세트는 결과
  화면에서 **☆ 킵**을 누르면 킵 화면의 「흐름」 칸에 모인다.
- 연표 줄 가운데 사건이 아닌 설명 카드(「왜 상하이였을까」, 「세계사적 의의」 등 26개, `OSKIP`)는 뺀다.

## 킵

수업 카드(카드 단계의 진행 막대 옆)와 인물·단체 카드에 **☆ 킵** 버튼이 있다. 누르면 `S.keep`에
`"L21#3"`(수업 · 카드 순번) 또는 `"N:n_kimgu"`(인물·단체)로 남는다. 버튼은 화면마다 따로 묶지 않고
`view` 한곳의 클릭 리스너가 받는다(`kToggle`).

홈에 「킵한 것 N개」 띠가 뜨고, 누르면 `vKeep`이 모아 보여 준다.
- 칩으로 전체 · 수업 · 인물·단체 · 빈칸을 거른다. 빈칸은 프린트 빈칸 탭에서 「헷갈림」을 누른 것(`S.pmark`)이다.
- 기본은 「가리고 보기」다. 머리말만 보이고, 눌러야 본문이 열린다. 「다 펴 놓기」로 바꿀 수 있다(`S.kpHide`).
- 「수업에서 보기」는 그 카드로 바로 간다. 풀어도 저절로 빠지지 않는다.
- 항목마다 「지우기」(빈칸은 `S.pmark`, 나머지는 `S.keep`에서 뺀다) — 5초 동안 되돌릴 수 있다.
  목록 아래 「이 칸 N개 전부 지우기」는 지금 칩에 보이는 것만, 두 번 눌러 지운다.
- 카드 순번으로 저장하니, 수업에 카드를 더할 때는 지금처럼 끝에만 붙여야 킵이 어긋나지 않는다.

## 인물·단체 카드

홈 '내신 대비'의 「인물·단체 카드」(`vNames` 설정 → `vNRun` 진행). 인물 55명 · 단체 58개(`NRAW` → `NAMES`)를
카드로 한 장씩 계속 맞히며 넘긴다.

| 필드 | 내용 |
|---|---|
| `NRAW[i]` | `[id, 이름, 인물/단체, 단원, 수업, 헷갈림 묶음, 단서들, 한 줄 정리]` |
| 단서 | `<b>…</b>` 낱말은 교과서·프린트 원문에 있는지 스크립트로 대조했다. 단서에 정답 이름은 넣지 않는다 |
| 헷갈림 묶음 | `kim`(김구·김원봉·김규식·김두봉·김좌진), `3bu`, `gun8`, `bomb`, `hist`, `front17`, `army17` 같은 이름표. 보기를 고를 때 가장 먼저 쓴다 |

**문제.** 한 카드가 두 방향으로 나온다 — 단서 → 이름(`|c`), 이름 → 맞는 설명(`|n`). 보기는
`nOpts(key)`가 같은 묶음 → 같은 단원 → 가까운 단원 순으로 뽑는데, 키를 씨앗으로 한 난수라 언제 만들어도
같다(오답 노트에 저장한 '내 답' 번호가 어긋나지 않는다). 이름 → 설명 문제에서는 정답의 굵은 낱말이나 이름이
들어간 설명을 보기에서 뺀다. 그것도 맞는 말일 수 있어서다. 화면에 낼 때는 `mixQ`로 섞는다.

**한 판.** 범위(전체 · 8~33쪽 · 34~63쪽)와 종류(섞기 · 인물 · 단체)를 고르면 약한 것 → 처음 보는 것 →
외운 것 순으로 20장을 뽑는다(`nDeal`).
- 맞히면 0.8초 뒤 다음 카드. 그 판에서 한 번에 맞히면 `S.nbox[id]`가 1 오르고 오답 노트에 「맞힘 ✓」를 붙인다.
  세 번 쌓이면(`nbox≥3`) 「외운 것」.
- **「모르겠어 · 넘기기」**는 틀린 것과 똑같이 처리한다(정답을 보여 주고, 3~5장 뒤에 다시, 오답 노트에도, `nbox` 0).
  오답 노트의 '내 답'은 비워 두고(`chose:null`), 판 끝에 넘긴 횟수(`R.sk`)를 보여 준다.
- 틀리면 멈추고 정답 · 한 줄 정리 · 「수업에서 보기」를 보여 준다. `nbox`는 0, 오답 노트에
  `wAdd("NAME","n",key)`, 그 카드는 **3~5장 뒤**에 다시 끼운다(남은 게 적으면 맨 끝). 다시 나와서 맞혀야 판에서 빠진다.
- 판 상태(`S.nrun`)를 저장해서 나갔다 와도 이어서 푼다. 끝나면 '한 번에 맞힘 · 다시 나와서 맞힘'과 틀렸던
  목록, 「틀린 것만 한 판 더」.

오답 노트는 `wItem`의 `kind:"n"`으로 같은 문제를 되살리고, 「인물·단체 카드」 묶음으로 보여 준다.

## 학교 프린트

광문고 한국사2 학습지(17쪽, 교과서 10~61쪽)를 교과서 원문과 한 줄씩 맞춰 본 결과가 두 군데에 들어 있다.

**수업 끝의 보충 카드.** 프린트에는 있는데 수업에 없던 내용을 카드 51장으로 만들어 해당 수업의
카드 배열 **맨 끝**에 붙였다(L1~L16 중 11개 수업, L21~L33). 중간에 끼우면 카드 순번으로 저장하는 진도(`S.pos`),
오답 노트, 암기 장치 키가 한 칸씩 밀리기 때문이다. 이 카드에는 `pr`(프린트 쪽수)이 있어서
「프린트 보충 · N쪽」 표시가 붙고, 교과서엔 아예 없는 표현이면 `po:1`을 더해 「프린트에만」으로
바뀐다(`prB`). 새 `fact` 카드 5장(미국의 참전, 독일의 오스트리아·체코슬로바키아 병합, 박재혁·최수봉, 김지섭,
조명하)은 연표에도 그대로 올라가고, `timeline()` 행이 `pr`·`po`를 들고 가서 연표 줄과 대조표 칸에도
「프린트」 표시가 붙는다. 「연표 스프린트」(index.html)에도 같은 다섯 사실을 `RAW` **맨 끝**에
붙였다(사실 id가 순번이라 중간에 넣으면 복습 기록이 엉킨다). `r[6]="pr"`이면 연표 줄에 「프린트」 꼬리표.

**홈 「학교 프린트 정리」**(`vPrint`, 자료는 `PRINT` · `PBLANK`). 탭 다섯 개 —

| 탭 | 내용 |
|---|---|
| 밑줄 | 선생님이 보충·삭제 표시로 밑줄 친 곳. PDF에서 밑줄 선을 좌표로 찾아보니 7쪽 교통국 문제와 8쪽 「(3) 삭제」 두 곳뿐이었다 |
| 빈칸 | 프린트 '내용 정리'에서 선생님이 괄호로 비운 핵심어 118개를 100문장에(`PBLANK`), '확인 문제' 2번의 ㉠·㉡ 고르기 · 빈칸 28문항을 그 아래에(`PCHK`) 담았다(`{답}` 자리가 가려진다). 문장을 누르면 열리고, 「헷갈림」 표시는 `S.pmark`에 남아 「헷갈림만」으로 모아 볼 수 있다. 괄호 안쪽을 띄어 쓴 것만 빈칸으로 봤다 — `(독일, …)` 같은 설명 괄호는 뺐다 |
| 차이 | 같은 내용을 프린트와 교과서가 다르게 적은 곳 11개. 프린트 / 교과서를 나란히 두고 시험용 한 줄을 단다 |
| 한쪽에만 | 프린트에만 있는 것(1쪽 세계사 배경, 은행령, 의열단 「소수 인원·적은 비용」, 민립 대학 구호 등)과 교과서에만 있는 것 |
| O·X | 프린트 확인 문제의 O·X 54문항. 눌러야 답이 열리고, × 문항만 걸러 볼 수 있다 |

각 줄의 「수업에서 보기」는 `l`(수업)과 `k`(카드 본문에 든 낱말)로 카드를 찾아 연다(`prFind`).
카드 순서가 바뀌어도 낱말로 찾으니 링크가 깨지지 않는다.

## 서술형 연습

내신은 고르는 문제만 나오지 않는다. 교과서 본문이 그대로 답이 되는 **28문항**을 넣었다
(`ESSAY`) — 8~33쪽 소단원 여덟 개에 두 개씩, 34~63쪽 소단원 열 개에 하나씩(`w19`~`w28`).
문장·핵심어·모범 답안 모두 교과서(8~63쪽) 본문 표현을
따랐고, 쓴 낱말이 교과서에 실제로 있는지 원문과 대조해 걸렀다.

| 필드 | 내용 |
|---|---|
| `q` | 문제 (「~서술하시오.」) |
| `keys[]` | 채점 기준이 되는 핵심어. `"가\|나"` 로 대체 표현을 적을 수 있다 |
| `model` | 모범 답안 |
| `tip` | 답을 어떤 순서로 쌓을지 알려 주는 한 줄 |

객관식은 고르면 끝이지만 서술형은 **무엇을 안 썼는지**를 알아야 는다. 그래서

1. 문제와 `tip` 을 먼저 보여 주고 글칸을 연다. 비워 두고 기준만 봐도 된다.
2. 「채점 기준 보기」를 누르면 쓴 글에서 핵심어를 **자동으로 찾아** 켠다
   (`whit()` — 띄어쓰기·가운뎃점·따옴표를 지우고 부분 일치로 본다).
3. 자동으로 못 잡은 것은 직접 눌러 켜고 끌 수 있다. 사람이 최종 채점자다.
4. 모범 답안에서 **넣은 말은 청록, 빠뜨린 말은 빨강**으로 짚어 준다.
   빠뜨린 말만 눈에 남는 게 이 화면의 전부다.

핵심어의 60% 이상을 넣으면 통과로 본다. 최고 점수는 `S.essay[문항id]` 에 남고 목록에
체크로 뜬다. 찾기 색인에도 들어가 있어서 낱말로 찾으면 그 문항으로 곧장 간다.

## 내신 실전

`EXAM` 배열에 학교 시험 유형 그대로 **140문항**을 담았다(34~63쪽 37문항 포함).

| type | 유형 | 개수 |
|---|---|---|
| `mc5` | 5지선다 | 38 |
| `boki` | &lt;보기&gt; ㄱㄴㄷㄹ 조합형 | 23 |
| `neg` | 옳지 않은 것 고르기 | 18 |
| `order` | 순서 배열 | 8 |
| `short` | 단답형 (서술형 대비) | 16 |

`boki`는 `boki`에 ㄱ~ㄹ 네 항목, `c`에 ①~⑤ 조합 선택지를 담는다.
`short`는 `c`가 허용 답안 목록이고 `a`는 `null`이다. 띄어쓰기와 문장 부호를
무시하고 비교하므로 "동양척식"과 "동양 척식 주식회사"가 모두 통과한다.

범위와 문항 수를 고르면 시험지 형태로 한 문항씩 나오고, **끝까지 풀고 한 번에
채점**한다. 하단 OMR 칸으로 아무 문항이나 오갈 수 있고 답을 고칠 수 있다.

채점 화면은 100점 환산 점수, 최고 점수 갱신, 유형별 정답률 막대, 그리고 약한
유형 한 개를 짚어 준다. 틀린 문항은 오답 노트로 넘어간다.

### 유형별 정답률 막대

색이 아니라 **길이**가 크기를 나른다. 처음엔 잘함/보통/약함을 초록·황토·빨강으로
칠하려 했으나, 검증기(`dataviz` 스킬)가 밝은 테마에서 빨강↔황토를 정상 시야
ΔE 11.9 (기준 15 미만, 하드 실패)로 잡았다. 색맹이 아니어도 구분이 어렵다는 뜻이라
단일 색으로 바꾸고 약한 유형은 「약점」 텍스트 배지로 표시한다.

약점은 **2문항 이상 푼 유형 중에서만** 고른다. 한 문항 틀린 걸 약점이라 부르지
않기 위해서다. 정답률이 같으면 표본이 큰 쪽을 고른다.

## 재미 요소

- **자라는 나무** — 마친 수업 수(0~20)가 그대로 성장 단계다. 씨앗 → 새싹 → 잎 →
  가지(12) → 꽃(15) → 열매(20). 프레임 높이가 나무 크기를 따라가서 초기 단계도
  허전해 보이지 않는다.
- **30초 복습 카드** — 마친 수업에서 암기 장치·사실·인과 사슬 중 한 장을 홈에 띄운다.
  ↻ 버튼으로 다시 뽑는다.
- **완료 도장** — 수업을 마치면 도장이 찍히는 연출.
- **연속 정답** — 퀴즈에서 3연속마다 "N연속!"이 뜬다.

## 저장

`localStorage`의 `hansa-learn-v1`. 마친 수업(`done`), 수업 중간 위치(`pos`),
퀴즈 점수(`quiz`), 오답(`wrong`), 복습 일정(`due`), 암기 자가 채점(`mem`),
시험일(`dday`), 내신 기록(`exam`), 연표 가리기(`tlHide`)와 설명 접기(`tlDesc`),
암기 장치 가리기(`mmHide`)와 좁히기(`mmOnly`), 서술형 최고 점수(`essay`),
연속 학습일을 담는다. 수업을 중간에 나가도 그
카드부터 이어서 열린다.

불러올 때 빠진 칸을 채우고 옛 저장본을 새 구조로 옮긴다. 앱이 자라도 예전 기록이
날아가지 않는다.

## 성능

세 앱 모두 단일 HTML 파일이라 눈에 띄는 병목이 둘 있었다. 둘 다 재서 고쳤다.

### 글꼴이 첫 화면을 막고 있었다

`<link rel="stylesheet">` 로 건 Google Fonts 가 렌더를 막는다. 망이 느리거나
막혀 있으면 **글꼴을 다 기다린 뒤에야** 화면이 뜬다 — 측정해 보니 첫 화면까지
**12.6초**였다.

링크를 `media="print"` 로 걸어 렌더에서 빼고, 다 받은 뒤에 `media="all"` 로
갈아 끼운다. 안 오면 대체 글꼴로 그냥 쓴다.

| 첫 화면까지(DOMContentLoaded) | 이전 | 지금 |
|---|---|---|
| `index.html` | 12,957ms | 60ms |
| `learn.html` | 12,574ms | 33ms |
| `monsters.html` | 12,589ms | 100ms |
| `hansa.html` | 12,931ms | 202ms |

### 긴 목록이 화면 밖까지 배치되고 있었다

치트키 26장·연표 105줄·찾기 결과 60개처럼 한 화면을 넘는 목록은, 안 보이는
부분까지 배치하느라 화면 전환이 느렸다. `content-visibility:auto` 로 화면 밖은
건너뛰게 하고 `contain-intrinsic-size` 로 자리만 잡아 둔다.

| 화면 전환 | 이전 | 지금 |
|---|---|---|
| 치트키 (26장) | 51.1ms | 2.9ms |
| 연표 (105줄) | 23.0ms | 3.7ms |
| 찾기 결과 (60개) | 49.5ms | 4.5ms |
| 인물 도감 (32장) | 3.7ms | 1.6ms |
| 몬스터 목록 (60마리) | 16.5ms | 6.2ms |

### 재 보고 안 고친 것

- **JS 는 이미 빨랐다.** 색인·문제 뽑기·정답 확인 전부 5ms 아래다.
  `buildIndex()` `timeline()` `choPool()` 은 이미 캐시하고 있었다.
- CSS 를 하나씩 꺼 가며 재 봤지만 `image-rendering` · `border-radius!important` ·
  하드 그림자 · 고정 배경 어느 것도 유의미하지 않았다. 같은 측정을 다섯 번 반복하니
  48.7 → 59.3ms 로 흔들려서, 그 정도 차이는 전부 측정 잡음이었다.
  **비용은 요소 개수였고, 답은 안 보이는 걸 안 그리는 것이었다.**

## 디자인 — HANSA OS

1985년식 픽셀 데스크톱 OS를 본떴다. 자둣빛 바탕 위에 크림색 창이 뜨고, 상단에는
가짜 OS 표시줄(로고 · 앱 이름 · 실시간 시계)이, 옆에는 장치 아이콘 독이 붙는다.

- **모서리 없음** — `border-radius:0!important`
- **3px 검정 테두리 · 블러 없는 4px 하드 그림자**
- **눌리는 버튼** — 안쪽 하이라이트/그림자로 입체를 내고, `:active`에 그림자만큼
  밀어 넣어 실제로 눌리게 한다
- **디더링 무늬** — `repeating-linear-gradient`로 줄무늬와 점선을 만든다
- `image-rendering:pixelated`

창 제목은 경로로 표시된다 (`/HOME` · `/LESSON/07` · `/EXAM/RUN`). 독은 넓은
화면에서 오른쪽 세로, 좁은 화면에서 아래 작업 표시줄이 된다. 독 아이콘 네 개는
16×16 격자에 `<rect>`를 찍어 그린 인라인 SVG다.

### 서체

한글 픽셀 폰트는 Google Fonts에 없다(갈무리·둥근모 모두 미제공). 그래서 나눠 쓴다.

| 용도 | 서체 |
|---|---|
| 영문·숫자 크롬 (창 제목, 시계, 번호, 라벨) | `Press Start 2P` |
| 한글 제목 | `Do Hyeon` |
| 한글 본문 | `Gothic A1` |

`Press Start 2P`는 같은 px 값에서 훨씬 크게 보이므로 7~11px 범위로 쓴다.

### 단일 세계관

라이트/다크 전환을 버리고 이 화면 하나로 통일했다. 모든 색을 직접 칠하므로
보는 사람의 테마와 무관하게 같은 모습이 나온다.

대비는 제목 14.13:1 · 본문 7.30:1 · 버튼 9.67:1 · 상단바 14.13:1로 전부 기준을
크게 넘긴다.

창 제목 표시줄의 버튼은 크롬 비례상 30px이지만 `::after`로 터치 영역만 46px로
넓혔다.

## 단체 계보도

홈 '복습'의 「단체 계보도」(`vOrgs`, 자료 `ORGMAP`). 단체가 어디서 나와 무엇과 합치고(통합) 갈라졌는지(분리)를 화살표 그림 9장으로 보여 준다 —
국외 기지 · 임시 정부 · 만주 ① · 만주 ② · 민족 유일당 운동과 신간회 · 중국 관내(김원봉 줄기 ↔ 김구 줄기) · 대중 운동 · 말·글·종교 · 광복 직전.
연결은 교과서 · 프린트 원문에 있는 것만 그렸다(예: 조선 노농 총동맹 분리는 교과서에 없어서 뺐다).
- 노드 `[id, 이름, 해·설명, 열, 행, 종류(o 단체 · e 사건 · g 정부 · l 칸 제목), 갈래(n 민족 · s 사회 · m 합작 · j 일제 · x 종교), 수업, 설명]`,
  화살표 `[from, to, 말, 선("" 이어짐 · d 점선 화살표 · r 관계)]`.
- `orgSVG`가 손으로 SVG를 그린다(viewBox 340 폭, 행 간격 92). 아래로는 직각으로 꺾고, 출발 줄 아래에 다른 상자가 있으면 바로 꺾어
  돌아간다. 화살촉은 `polygon`이라 합본에서 id가 부딪히지 않는다. 색은 페이지 CSS 변수.
- 상자를 누르면 설명(인물·단체 카드에 있으면 그 단서)과 「수업에서 보기」. 「이름 가리기」(`S.orgHide`)로 상자 이름을 가리고 하나씩 열어 볼 수 있다.
- 인물·단체 카드 화면 위와, 단체 카드를 틀렸을 때 「계보도에서 보기」로 들어온다(`go("orgs",{name})`).
- NAMES의 단체 58개는 모두 그림에 있다(`t-org` 검사: 화살표 끝 · 겹침 · 글자 넘침 · 선이 상자를 지나가는지).

## 시간순 복습

홈 '복습' 맨 위 「시간순으로 쭉 복습」(`vChron`). 첫 수업의 사실 카드 전부(165개)를 `timeline()` 순서(연도 → 1919 시기 구분 → 같은 해 월일)로
한 페이지에 쭉 늘어놓는다. 큰 묶음 여섯 개(`BLOCKS`)가 장이다 — 장 머리에 그때 일제 통치(`g`)와 ▶ 여는 · ◀ 닫는 사건.
- 사건 카드: 연도(대략 시기는 `ya` 말) · 갈래 · 본문 · 왜? · 그래서 · 프린트 배지 · ☆ 킵 · 「수업에서 보기」. 해가 바뀔 때만 연도 줄.
- 장 끝에 그 시기 수업의 암기 장치 · 비교 · 인과 사슬(「○장 정리」)과 확인 · 2지선다(「○장 확인」). 수업은 사실 카드가 가장 많이 속한
  묶음 한 곳에만 배정한다(`chronLessonBlock`) — 정리 · 확인 카드 185장이 정확히 한 번씩 나온다.
- 맨 위 붙박이 줄: 장 칩(누르면 이동, 스크롤을 따라 켜짐), 「한 줄로 보기」(`S.chronMode`), 「연도 가리기」(`S.chronHideY`, 누르면 하나씩).
- 마지막 본 장을 `S.chron`에 남겨 홈에서 「○장부터 이어 읽기」. 누르기(연도 · 답 · 2지선다)는 그 자리만 바꿔 긴 페이지를 다시 그리지 않는다.
- 이 화면의 카드는 등장 애니메이션을 끈다(`content-visibility`로 화면에 들어올 때마다 다시 돌아 깜빡이므로).

