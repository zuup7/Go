{
  "name": "family-go",
  "version": "0.1.0",
  "description": "Family Go! - 대를 잇는 인생 시뮬레이션 게임 / 차트런 - 음원차트 1위 픽셀 플랫포머",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.js chartrun/tests/*.test.js",
    "build": "node build.mjs && node build.mjs chartrun && node build.mjs chartrun --artifact",
    "build:chart": "node build.mjs chartrun",
    "build:artifact": "node build.mjs chartrun --artifact"
  },
  "license": "MIT"
}
