# 오답 몬스터

고1 한국사 기말 대비 퀴즈 게임. `monsters.html` 단일 파일이며 외부 라이브러리와
빌드 도구 없이 vanilla JS로 돌아간다. 아이콘과 캐릭터는 전부 인라인 SVG다.

## 문제 데이터

파일 상단 `QUESTIONS` 배열 하나에 모여 있다. 이 배열만 갈아 끼우면 범위가 바뀐다.

```js
{ id, unit, type, question, choices, answer, explain, era }
```

| type | choices | answer |
|---|---|---|
| `ox` | `["O","X"]` | 0 또는 1 |
| `mc` | 보기 4개 | 정답 인덱스 |
| `order` | `"연도\|사건"` 문자열을 **올바른 순서로** | `null` (실행 시점에 섞는다) |

`id`는 `단원-사실번호-유형` 규칙이다. 앞 두 마디가 같으면 같은 사실을 다르게 물은
문제로 보고 한 판에 한 번만 출제한다.

현재 386문제 — 4지선다 279, O·X 51, 사료 24, 키워드 20, 연표 12세트.

`source`(사료 제시형)는 `mc`와 같은 구조이고 `question`에 사료 원문이 들어간다.
`keyword`(키워드 입력형)는 `choices`의 각 항목이 필수 키워드 하나이며 `|` 뒤는
허용 표기다. `answer`는 `null`이고 세 칸을 모두 맞혀야 통과한다.

## 출제 범위

2022 개정 한국사2 Ⅰ단원 「일제 식민 통치와 민족 운동」 01~03-1 (교과서 8~33쪽).
`unit` 값과 `era`(스토리용 시대명)는 소단원 8개에 1:1로 대응한다.

| unit | 소단원 | era |
|---|---|---|
| `s1` | 20세기 전반의 세계 | 세계대전의 소용돌이 |
| `s2` | 무단 통치와 수탈 체제 | 칼을 찬 교사의 시대 |
| `s3` | 민족 분열 통치와 경제 수탈 | 가면을 쓴 통치 |
| `s4` | 민족 말살 통치와 전시 동원 | 이름을 빼앗긴 시대 |
| `s5` | 1910년대 국내외 독립운동 | 국경을 넘은 사람들 |
| `s6` | 3·1 운동 | 만세의 봄 |
| `s7` | 대한민국 임시 정부 | 황제의 나라에서 국민의 나라로 |
| `s8` | 무장 독립 전쟁 | 봉오동에서 대전자령까지 |

## 모드

- **스피드 O·X** — 60초. 정답마다 `(40 + 남은초 × 2) × (1 + min(콤보,10) × 0.15)`점.
  해설을 붙잡지 않고 바로 넘어가며 복습은 결과 화면에서 한다.
- **4지선다** — 단원을 고르고 10문제. 해설을 읽고 직접 넘긴다.
- **연표 세우기** — 사건 카드 4~5장. 연도는 채점할 때 공개된다.
- **듣기 모드** — Web Speech API. 문제 → 정답 → 해설을 이어 읽고 자동으로 넘어간다.

## 오답 몬스터

틀리면 그 문제가 `S.monsters[id] = {hp, at}`로 등록된다. `buildRound()`가 몬스터를
맨 앞에 배치하고, **3번 연속** 맞히면 소멸한다. 한 번이라도 틀리면 `hp`가 0으로
돌아간다. 몬스터는 우선 복습 대상이라 같은 사실 중복 제거를 건너뛴다.

생김새는 `id`를 FNV-1a로 해시해 색상·눈 개수·뿔 모양을 정하므로 같은 문제는 늘 같은
몬스터로 나온다.

## 모바일에서 실제로 문제가 되는 자리

- **드래그** — HTML5 drag & drop은 터치에서 동작하지 않아 Pointer Events로 직접
  구현했다. 화살표 버튼도 함께 둬서 드래그가 안 되는 환경에서도 풀 수 있다.
- **진동** — iOS 사파리는 `navigator.vibrate`를 지원하지 않는다. 진동 없이도
  색과 해설로 정답 여부가 구분된다.
- **매니페스트** — 단일 파일 조건 때문에 별도 `manifest.json`을 둘 수 없어 실행
  시점에 Blob URL로 만들어 붙인다. iOS는 `apple-mobile-web-app-*` 메타태그로 처리.
- **음성** — iOS는 사용자 제스처 없이 다음 문장을 이어 읽지 못하고 화면이 꺼지면
  멈춘다. 시작 버튼을 누르면 그 세션 동안 `onend` 체인으로 이어 간다. 한국어 음성이
  없는 기기에서는 안내문을 띄운다.

## 저장

`localStorage`의 `hansa-monster-v1` 하나에 몬스터·단원별 정답률·누적 정답·연속
학습일·최고 점수를 담는다.

## 스토리 모드

`ERA_ORDER`대로 소단원 하나가 시대 하나다. 보스는 사람이 아니라 그 시대를 짓눌렀던
제도와 정책이다(무단 통치 = 헌병의 칼, 문화 정치 = 가면).

데미지는 `8 + 응답속도보너스(0~8)`이고 3연속 정답마다 2.2배 필살기가 터진다.
체력은 240~320. 빠르고 정확하면 13~16문제, 보통 17~22문제, 느리고 부정확하면
34~46문제가 걸린다. 문제 풀이 바닥나면 후퇴 화면으로 간다.

격파하면 다음 시대가 해금되고 지도에 색이 칠해진다.

## 인물 도감

`FIGURES` 32명 (SR 6 · R 10 · N 16). 정답 10개마다 아직 없는 카드를 한 장 뽑는다.
등급별 가중치는 `GRADE[].p`. 초상은 `portrait()`가 머리·수염·안경·모자·옷 다섯
가지 특징 조합으로 그리는 도식 일러스트이며 사진이 아니다.

타이머가 도는 스피드 O·X 중에는 카드 연출을 결과 화면으로 미룬다. 모달이 뜬 사이
시간이 흐르면 불리하기 때문이다.

## 아이템

`5:5 소거` · `시간 +10초` · `부활`. 정답 7% 확률, 미션 완료, 보스 첫 격파로만 얻는다
(오답으로는 절대 나오지 않는다). 문제 화면 위 아이템 바에서 쓴다.

## 일일 요소

출석 도장 14칸, 오늘의 미션 3개(날짜 해시로 고정 선택), 시험 D-day.

D-day를 넣으면 `monsterQuota()`가 한 판의 몬스터 비중을 조절한다 — D-30 이상 40%,
D-14~29 60%, D-7~13 80%, D-6 이하 100%. **D-day를 넣지 않으면 1단계와 똑같이
몬스터를 전부 우선 출제한다.**

## 2단계를 붙인 방식

기존 함수는 그대로 두고 감싸기만 했다. `paintQ` `hud` `grade` `next` `finish`
`vPlay` `vHome` `vResult`를 각각 `_원본`에 담아 두고 새 동작을 앞뒤에 덧댄다.
화면은 `VIEWS` 레지스트리에 등록한다. 1단계 코드에서 바꾼 것은 두 줄뿐이다 —
`render()`의 화면 표를 `VIEWS`로 뺀 것과, `buildRound()`에 `monsterQuota` 훅을
넣은 것.

`.era`는 1단계가 이미 문제 화면의 시대 라벨로 쓰고 있어 지도 카드는 `.eracard`를
쓴다. 두 `<style>` 블록 사이에 겹치는 최상위 클래스가 없는지 확인했다.

## 3단계 — 새 게임 모드 셋

홈 「한 판 더」 칸에 붙는다. 기존 데이터를 다시 쓰기 때문에 문제를 새로 쓰지 않았다.

### 초성 사냥

`ㅂㅇㄷ ㅈㅌ` 만 보고 「봉오동 전투」를 맞힌다. 설명이 힌트로 함께 나온다.
한 판 10문제.

낱말은 `-ev`(설명 → 사건명)와 `-bl`(빈칸 채우기) 문제의 정답에서 뽑는다 — **210개**.
한글이 두 자 이상이어야 하고 16자를 넘으면 제외한다.

- **한 글자 열기** — 왼쪽부터 한 글자씩 실제 글자로 바꾼다. 열 때마다 30점씩 깎이고
  마지막 한 글자는 열리지 않는다.
- 점수 = `100 + (콤보−1)×20 − 힌트×30`, 최소 20점.
- 띄어쓰기·가운뎃점·따옴표를 무시하고 견주므로 "치안유지법"도 통과한다.
- 시대 이름(`era`)은 안 보여 준다. 「봉오동에서 대전자령까지」처럼 답을 그대로 품는
  경우가 있어서다. 교과서 쪽수만 띄운다.

### 짝 맞추기

카드 12장(6쌍)을 뒤집어 **사건 이름과 그 해**를 짝짓는다. 짝은 `-yr` 문제
(「○○」은(는) 몇 년의 일일까?)에서 뽑는다 — **69쌍**.

- 한 판 안에서 연도도 사건도 겹치지 않게 고른다. 안 그러면 짝이 애매해진다.
- 기록은 **걸린 시간**이고 같으면 뒤집은 횟수가 적은 쪽이 이긴다.
- 다 맞히면 그 판의 여섯 사건을 **연도순으로 세워** 보여 준다. 이게 이 판의 배움이다.

### 서바이벌

목숨 셋. 4지선다와 O·X 가 섞여 끝없이 나오고 세 번 틀리면 끝. 기록은 **버틴 문항 수**.

기존 문제 루프(`paintQ` · `grade` · `next`)를 그대로 쓰고 감싸기만 했다. 그래서
아이템도 그대로 통한다 — **부활**을 쓰면 목숨이 깎이지 않는다.

### 명예의 전당

모드별 최고 기록을 한 곳에 모은다. `S.best = {ox, cho, sv, pair:{t,m}}`.
`s3init()`이 없는 칸을 채우므로 1·2단계 저장본을 그대로 이어 쓴다.

미션도 셋 늘었다 — 초성 사냥 한 판 · 짝 맞추기 한 판 · 서바이벌 10문제.

### 3단계를 붙인 방식

2단계와 같다. `hud` `grade` `next` `finish` `vPlay` `vResult` `vHome`을 각각
`_원본3`에 담아 두고 앞뒤에 덧댄다. 초성 사냥과 짝 맞추기는 문제 루프를 쓰지 않으므로
`VIEWS`에 자기 화면(`cho` · `pair` · `hall`)으로 등록했다. 짝 맞추기는 상태를 `G`가
아니라 `P`에 따로 담는다 — 문제 큐가 없기 때문이다.

부팅이 이미 홈을 그린 뒤에 이 블록이 실행되므로, 끝에서 홈이면 한 번 더 그린다.

## 4단계 — 먼저 온 쪽 · 오늘의 한 판

### 먼저 온 쪽

두 사건을 나란히 놓고 **어느 쪽이 먼저 일어났는지**만 고른다. 60초 안에 최대한 많이.

연도를 물어보던 문제를 걷어낸 자리에서 나온 게임이다. 연도는 채점에만 쓰고 화면에는
고른 뒤에야 보여 준다 — 몇 년인지 몰라도, 순서만 알면 이긴다.

- 사건 목록 **74개**(1910~1944, 서로 다른 30개 해)를 `-yr` 문제와 `order` 세트에서
  뽑아 만든다. 「대한인 국민회」와 「대한인 국민회 결성」처럼 한쪽이 다른 쪽을 품는
  이름은 하나만 남긴다.
- 같은 해끼리는 짝을 짓지 않는다. 동전 던지기가 되기 때문이다.
- 점수 `50 + min(콤보,10)×10`. **틀리면 3초를 잃는다** — 이게 긴장을 만든다.

### 오늘의 한 판

날짜로 고정한 **열 문제**. 같은 날이면 언제 열어도 같은 문제가 나오고, 하루 한 번만
푼다. 홈 맨 위와 「오늘」 화면 맨 위에 붙는다.

`hash("chal" + 날짜 + i)` 로 고른다. `S.chal = {d, done, ok, n}` 에 그날 결과가 남고,
날이 바뀌면 자동으로 새 판이 열린다.

### 시계 정리

3·4단계가 각자 `setInterval`/`setTimeout` 을 쓴다. 화면을 떠났는데 시계가 계속 돌면
남의 화면 위에 결과를 그려 버린다. 그래서

- `stopAll()` 을 감싸 듀얼·짝 맞추기 시계를 함께 멈추고,
- 각 콜백은 **지금 화면이 그 게임인지 확인한 뒤에만** 그린다.

`.win` 은 셸의 창 클래스라 카드 상태 이름으로 쓸 수 없다 (세로 flex 가 덮어씌워진다).
`.dwin` / `.dlose` 를 쓴다.

## 5단계 — 시간을 달리다 (갈림길 러너)

캔버스 위에서 도는 **멈출 수 없는 러너**. 처음엔 침략자를 쏘는 슈팅으로 만들었는데
손이 할 일이 없었다 — 총은 자동으로 나가고 적은 0.9초에 한 칸씩 움직이니 결국
「느린 객관식 + 배경화면」이었다. 그래서 장르를 바꿨다.

- 두 갈래 길을 계속 달린다. 멈출 수 없다.
- 위에 **사건 이름 하나**가 뜬다. 갈림길의 두 문에는 **시대 이름**이 걸려 있다.
- 그 사건이 속한 시대의 문으로 달려 들어가야 한다.
- 틀리면 넘어진다 — 화면이 흔들리고, 잠깐 느려지고, 「그건 · 무단 통치」 하고 알려 준다.
- 목숨 셋. 문 사이에 뿌려진 별은 주우면 점수.
- **달릴수록 빨라진다.** 갈림길 간격이 1.8초에서 0.8초까지 줄어든다.

읽을 것이 **하나**라서 빠르게 굴릴 수 있다. 앞 판(아홉 개 이름표를 읽고 고르기)이
느렸던 이유가 이거였다.

| | |
|---|---|
| 사건 이름 | `-ev`(설명 → 사건명) 문제의 정답. 14자 넘으면 뺀다 |
| 문패 | 시대 이름을 줄여 쓴다 — 무단 통치 · 문화 정치 · 민족 말살 · 1910년대 · 3·1 운동 · 임시 정부 · 무장 투쟁 · 세계사 |
| 조작 | 화면 좌/우 탭, PC 는 ← → |
| 속도 | `0.032 → 0.072` 칸/ms. 길 줄무늬가 속도만큼 길어져 달리는 느낌을 만든다 |
| 끝나면 | 틀린 것을 **「헷갈린 것」** 으로 시대와 함께 모아 보여 준다 |

### 캔버스

가로 64칸 격자. 세로가 화면을 다 먹지 않게 폭을 `창 높이의 60%` 에 맞춰 줄이고
가운데 둔다. 달리는 사람은 두 칸 프레임으로 다리를 바꾸고 위아래로 통통 튄다 —
이게 없으면 그냥 미끄러지는 그림이 된다.

### 멈추고 이어 하기

`requestAnimationFrame` 이 도는 화면이라 나갈 때 확실히 멈춰야 한다.

- 화면을 벗어나면(`stack` 확인) 루프를 끊고 키 리스너도 뗀다. `stopAll()` 도 감쌌다.
- 합친 앱(`hansa.html`)에서 다른 앱으로 갈아 끼우면 이 화면이 DOM 에서 **떨어진다.**
  그때는 루프를 끊지 않고 **쉰다** — 보고 있지도 않은데 갈림길이 지나가면 억울하다.
  3초를 떠나 있어도 6m 밖에 안 나가고, 돌아오면 그 자리에서 이어진다.

## 성능

세 앱 모두 단일 HTML 파일이라 눈에 띄는 병목이 둘 있었다. 둘 다 재서 고쳤다.

### 글꼴이 첫 화면을 막고 있었다

`<link rel="stylesheet">` 로 건 Google Fonts 가 렌더를 막는다. 망이 느리거나
막혀 있으면 **글꼴을 다 기다린 뒤에야** 화면이 뜬다 — 측정해 보니 첫 화면까지
**12.6초**였다.

링크를 `media="print"` 로 걸어 렌더에서 빼고, 다 받은 뒤에 `media="all"` 로
갈아 끼운다. 안 오면 대체 글꼴로 그냥 쓴다.

| 첫 화면까지(DOMContentLoaded) | 이전 | 지금 |
|---|---|---|
| `index.html` | 12,957ms | 60ms |
| `learn.html` | 12,574ms | 33ms |
| `monsters.html` | 12,589ms | 100ms |
| `hansa.html` | 12,931ms | 202ms |

### 긴 목록이 화면 밖까지 배치되고 있었다

치트키 26장·연표 105줄·찾기 결과 60개처럼 한 화면을 넘는 목록은, 안 보이는
부분까지 배치하느라 화면 전환이 느렸다. `content-visibility:auto` 로 화면 밖은
건너뛰게 하고 `contain-intrinsic-size` 로 자리만 잡아 둔다.

| 화면 전환 | 이전 | 지금 |
|---|---|---|
| 치트키 (26장) | 51.1ms | 2.9ms |
| 연표 (105줄) | 23.0ms | 3.7ms |
| 찾기 결과 (60개) | 49.5ms | 4.5ms |
| 인물 도감 (32장) | 3.7ms | 1.6ms |
| 몬스터 목록 (60마리) | 16.5ms | 6.2ms |

### 재 보고 안 고친 것

- **JS 는 이미 빨랐다.** 색인·문제 뽑기·정답 확인 전부 5ms 아래다.
  `buildIndex()` `timeline()` `choPool()` 은 이미 캐시하고 있었다.
- CSS 를 하나씩 꺼 가며 재 봤지만 `image-rendering` · `border-radius!important` ·
  하드 그림자 · 고정 배경 어느 것도 유의미하지 않았다. 같은 측정을 다섯 번 반복하니
  48.7 → 59.3ms 로 흔들려서, 그 정도 차이는 전부 측정 잡음이었다.
  **비용은 요소 개수였고, 답은 안 보이는 걸 안 그리는 것이었다.**

## 디자인 — HANSA OS

세 앱(`index.html` · `monsters.html` · `learn.html`)이 같은 껍데기를 쓴다. 1985년식
픽셀 데스크톱 OS를 본떠, 자둣빛 바탕 위에 창이 뜨고 위에는 가짜 OS 표시줄(로고 ·
앱 이름 · 실시간 시계)이 붙는다. 창 제목은 경로로 표시된다 (`/HOME` · `/MAP/BOSS` ·
`/PLAY` · `/DEX` · `/DAILY`).

**창 안은 어둡게 두었다.** 이 앱은 몬스터 전투 게임이고 인라인 SVG 몬스터·보스·초상
전부가 어두운 배경 위에서 그려졌다. 같은 OS 에서 돌아가는 게임 화면이라는 뜻이 된다.

- **모서리 없음** — `border-radius:0!important`
- **3px 테두리(`--line` 근검정) · 블러 없는 4px 하드 그림자**
- **눌리는 버튼** — 안쪽 하이라이트/그림자로 입체를 내고 `:active`에 그림자만큼 밀어 넣는다
- 체력·진행 막대는 `repeating-linear-gradient`로 눈금을 새긴 픽셀 막대다

어두운 판때기 위에서는 `--ink`(창틀 색)가 배경과 같은 색이라 테두리가 사라진다.
그래서 창 안 부품은 전부 `--line`(#150E1B)으로 두른다.

### 서체

| 용도 | 서체 |
|---|---|
| 영문·숫자 크롬 (창 제목, 시계, 점수, 라벨) | `Press Start 2P` |
| 한글 제목 | `Do Hyeon` |
| 한글 본문 | `Gothic A1` |

`Press Start 2P`는 같은 px에서 훨씬 크게 보이므로 크롬 글씨를 7~11px로 줄였다.

### 창 제목

화면마다 `titleEl.textContent`에 한글 제목을 넣던 코드가 열두 군데 있는데, 그중
일부는 `render()`를 거치지 않고 다시 그린다. 그래서 `titleEl`을 값을 흘려보내는
객체로 바꾸고 `render()`에서만 실제 제목(`titleReal`)에 경로를 쓴다. 호출부를 열두
군데 고치는 것보다 한 군데서 막는 편이 확실하다.

### 대비

본문 11.62:1 · 잔글씨 4.96:1 · 구역 제목 17.13:1 · 상단바 14.13:1. 360px·820px
양쪽에서 열 화면 전부 가로 넘침 없음, 28px 미만 터치 영역 없음.
