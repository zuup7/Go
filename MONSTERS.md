# 오답 몬스터

고1 한국사 기말 대비 퀴즈 게임. `monsters.html` 단일 파일이며 외부 라이브러리와
빌드 도구 없이 vanilla JS로 돌아간다. 아이콘과 캐릭터는 전부 인라인 SVG다.

## 문제 데이터

파일 상단 `QUESTIONS` 배열 하나에 모여 있다. 이 배열만 갈아 끼우면 범위가 바뀐다.

```js
{ id, unit, type, question, choices, answer, explain, era }
```

| type | choices | answer |
|---|---|---|
| `ox` | `["O","X"]` | 0 또는 1 |
| `mc` | 보기 4개 | 정답 인덱스 |
| `order` | `"연도\|사건"` 문자열을 **올바른 순서로** | `null` (실행 시점에 섞는다) |

`id`는 `단원-사실번호-유형` 규칙이다. 앞 두 마디가 같으면 같은 사실을 다르게 물은
문제로 보고 한 판에 한 번만 출제한다.

현재 342문제 — 4지선다 279, O·X 51, 연표 12세트.

## 출제 범위

2022 개정 한국사2 Ⅰ단원 「일제 식민 통치와 민족 운동」 01~03-1 (교과서 8~33쪽).
`unit` 값과 `era`(스토리용 시대명)는 소단원 8개에 1:1로 대응한다.

| unit | 소단원 | era |
|---|---|---|
| `s1` | 20세기 전반의 세계 | 세계대전의 소용돌이 |
| `s2` | 무단 통치와 수탈 체제 | 칼을 찬 교사의 시대 |
| `s3` | 민족 분열 통치와 경제 수탈 | 가면을 쓴 통치 |
| `s4` | 민족 말살 통치와 전시 동원 | 이름을 빼앗긴 시대 |
| `s5` | 1910년대 국내외 독립운동 | 국경을 넘은 사람들 |
| `s6` | 3·1 운동 | 만세의 봄 |
| `s7` | 대한민국 임시 정부 | 황제의 나라에서 국민의 나라로 |
| `s8` | 무장 독립 전쟁 | 봉오동에서 대전자령까지 |

## 모드

- **스피드 O·X** — 60초. 정답마다 `(40 + 남은초 × 2) × (1 + min(콤보,10) × 0.15)`점.
  해설을 붙잡지 않고 바로 넘어가며 복습은 결과 화면에서 한다.
- **4지선다** — 단원을 고르고 10문제. 해설을 읽고 직접 넘긴다.
- **연표 세우기** — 사건 카드 4~5장. 연도는 채점할 때 공개된다.
- **듣기 모드** — Web Speech API. 문제 → 정답 → 해설을 이어 읽고 자동으로 넘어간다.

## 오답 몬스터

틀리면 그 문제가 `S.monsters[id] = {hp, at}`로 등록된다. `buildRound()`가 몬스터를
맨 앞에 배치하고, **3번 연속** 맞히면 소멸한다. 한 번이라도 틀리면 `hp`가 0으로
돌아간다. 몬스터는 우선 복습 대상이라 같은 사실 중복 제거를 건너뛴다.

생김새는 `id`를 FNV-1a로 해시해 색상·눈 개수·뿔 모양을 정하므로 같은 문제는 늘 같은
몬스터로 나온다.

## 모바일에서 실제로 문제가 되는 자리

- **드래그** — HTML5 drag & drop은 터치에서 동작하지 않아 Pointer Events로 직접
  구현했다. 화살표 버튼도 함께 둬서 드래그가 안 되는 환경에서도 풀 수 있다.
- **진동** — iOS 사파리는 `navigator.vibrate`를 지원하지 않는다. 진동 없이도
  색과 해설로 정답 여부가 구분된다.
- **매니페스트** — 단일 파일 조건 때문에 별도 `manifest.json`을 둘 수 없어 실행
  시점에 Blob URL로 만들어 붙인다. iOS는 `apple-mobile-web-app-*` 메타태그로 처리.
- **음성** — iOS는 사용자 제스처 없이 다음 문장을 이어 읽지 못하고 화면이 꺼지면
  멈춘다. 시작 버튼을 누르면 그 세션 동안 `onend` 체인으로 이어 간다. 한국어 음성이
  없는 기기에서는 안내문을 띄운다.

## 저장

`localStorage`의 `hansa-monster-v1` 하나에 몬스터·단원별 정답률·누적 정답·연속
학습일·최고 점수를 담는다.

## 다음 단계

스토리 모드(보스전), 인물 도감, 아이템, 일일 미션, 사료 제시형·키워드 입력형 문제는
아직 붙이지 않았다.
